"""
Package: scripts
Purpose:
- Entry points and utilities runnable from the command line during development.
Notes:
- Prefer running as a module (e.g., `python -m scripts.gen_synth`) so package imports like `sniper.io.*` resolve.
"""