# Toggle DuckDB writes: 0 = disable (Parquet-only), 1 = enable (write to DuckDB)
SNIPER_USE_DUCKDB=0

# Optional: override DuckDB database file path (absolute path recommended)
# SNIPER_DUCKDB_PATH=d:/Games/Sniper_ML/data/processed/sniper_db.duckdb

# Optional: runtime logging level for scripts (e.g., DEBUG, INFO, WARNING)
# SNIPER_LOG_LEVEL=INFO