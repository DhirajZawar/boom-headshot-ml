# Global toggles
# - Set to 0 to disable DuckDB writes and use Parquet-only pipeline
# - Set to 1 (or omit) to enable DuckDB writes
SNIPER_USE_DUCKDB=0

# Database paths (override only if you move folders)
# SNIPER_DUCKDB_PATH=d:/Games/Sniper_ML/data/processed/sniper_db.duckdb

# Runtime options (future use)
# SNIPER_LOG_LEVEL=INFO
# SNIPER_MAX_PRODUCTS=5000